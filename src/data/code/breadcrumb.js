export const breadcrumb = `
    import { Breadcrumb, BreadcrumbItem } from "components/Breadcrumb";

    return(
      <Breadcrumb>
        <BreadcrumbItem label="item1" path="#" />
        <BreadcrumbItem label="item2" path="#" />
        <BreadcrumbItem label="current page" current/>
      </Breadcrumb>
    )
`;
